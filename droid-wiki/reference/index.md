# Reference

This section is the index for the reference pages: configuration, data models, and dependencies. Each page is a map that links out to the systems and primitives pages rather than re-explaining the models in depth.

| Page | Summary |
|------|---------|
| [Configuration](configuration.md) | `pyproject.toml`, kernel version pins, the default convention block, environment variables, and frontend configs. |
| [Data models](data-models.md) | The pydantic models that flow through the system, with the file they live in and the page that explains them. |
| [Dependencies](dependencies.md) | Python dependency groups, the external Cadabra2 tool, the ambient-auth LLM runtime, and the frontend stack. |
